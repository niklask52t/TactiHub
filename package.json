{
  "name": "tactihub",
  "version": "1.3.0",
  "private": true,
  "description": "Tactical strategy planning for competitive games",
  "scripts": {
    "dev": "concurrently \"pnpm --filter @tactihub/server dev\" \"pnpm --filter @tactihub/client dev\"",
    "build": "pnpm --filter @tactihub/shared build && pnpm --filter @tactihub/server build && pnpm --filter @tactihub/client build",
    "db:generate": "pnpm --filter @tactihub/server db:generate",
    "db:migrate": "pnpm --filter @tactihub/server db:migrate",
    "db:seed": "pnpm --filter @tactihub/server db:seed",
    "db:studio": "pnpm --filter @tactihub/server db:studio"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "bcrypt",
      "sharp",
      "esbuild"
    ]
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "typescript": "^5.7.3"
  }
}
